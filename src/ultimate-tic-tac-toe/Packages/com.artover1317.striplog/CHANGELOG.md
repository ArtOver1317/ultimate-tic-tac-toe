# Changelog

## 0.2.0

- Implemented core API: `Log`, `LogLevel`, `ILogHandler`, `UnityLogHandler`, `ScopedLogger`.
- Added runtime controls: `MinLevel`, `MuteTag/UnmuteTag/IsTagMuted`, `Handler` (null-guard).
- Added Samples: BasicTags, ColoredOutput, ScriptableSettings, optional R3 integration.
- Added EditMode tests (Editor-only) for core behavior.

## 0.1.0

- Initial scaffold (UPM structure, asmdef placeholder).
