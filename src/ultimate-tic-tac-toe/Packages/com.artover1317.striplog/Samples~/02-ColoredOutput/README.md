# 02-ColoredOutput

What it shows:
- A custom `ILogHandler` that colors tags using Unity rich text

How to try:
- Add `ColoredOutputBootstrap` to any GameObject in a scene
- In Editor / Development Build it sets `Log.Handler = new ColoredOutputLogHandler()`
