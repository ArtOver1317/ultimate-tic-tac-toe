# 04-R3Integration

What it shows:
- Optional R3 integration using a `partial` hook in `Log`

How to enable:
- Install R3 in your project
- Add scripting define `LOG_R3_SUPPORT`

How to try:
- Add `R3LogListenerExample` to any GameObject
- Subscribe to `Log.OnLog`
